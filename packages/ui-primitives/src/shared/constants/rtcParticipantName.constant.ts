/**
 * Fallback display names for RTC / third-party call SDK user fields.
 * Keep fixed (not i18n): locale must not change identifiers sent to providers.
 */
export const RTC_FALLBACK_LOCAL_DISPLAY_NAME = 'Patient';
export const RTC_FALLBACK_REMOTE_DISPLAY_NAME = 'Doctor';
